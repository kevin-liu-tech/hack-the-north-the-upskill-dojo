// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
import { makeDetectLanguageResult, makeDetectLanguageErrorResult } from "./detectLanguageResult";
import { sortResponseIdObjects } from "./util";
export function makeDetectLanguageResultArray(input, documents, errors, modelVersion, statistics) {
    const unsortedResult = documents
        .map((document) => {
        return makeDetectLanguageResult(document.id, document.detectedLanguage, document.warnings, document.statistics);
    })
        .concat(errors.map((error) => {
        return makeDetectLanguageErrorResult(error.id, error.error);
    }));
    const result = sortResponseIdObjects(input, unsortedResult);
    return Object.assign(result, {
        statistics,
        modelVersion
    });
}
//# sourceMappingURL=detectLanguageResultArray.js.map