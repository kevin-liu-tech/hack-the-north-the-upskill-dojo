// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
import { makeExtractKeyPhrasesResult, makeExtractKeyPhrasesErrorResult } from "./extractKeyPhrasesResult";
import { sortResponseIdObjects } from "./util";
export function makeExtractKeyPhrasesResultArray(input, documents, errors, modelVersion, statistics) {
    const unsortedResult = documents
        .map((document) => {
        return makeExtractKeyPhrasesResult(document.id, document.keyPhrases, document.warnings, document.statistics);
    })
        .concat(errors.map((error) => {
        return makeExtractKeyPhrasesErrorResult(error.id, error.error);
    }));
    const result = sortResponseIdObjects(input, unsortedResult);
    return Object.assign(result, {
        statistics,
        modelVersion
    });
}
//# sourceMappingURL=extractKeyPhrasesResultArray.js.map